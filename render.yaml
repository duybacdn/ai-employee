services:
  - type: web
    name: ai-employee-dashboard
    env: static
    buildCommand: cd ai-employee-dashboard && npm install && npm run build
    staticPublishPath: ai-employee-dashboard/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html